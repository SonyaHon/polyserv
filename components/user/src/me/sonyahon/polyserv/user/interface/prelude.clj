(ns me.sonyahon.polyserv.user.interface.prelude
  (:require [me.sonyahon.polyserv.user.prelude :as prelude]))
(defn prelude
  "Ensures all necessary things are done for the user component"
  []
  (prelude/prelude))
